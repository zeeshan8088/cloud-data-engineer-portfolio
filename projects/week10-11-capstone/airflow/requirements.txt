# ============================================================
# RetailFlow — Airflow Worker Requirements
# ============================================================
# Installed into the custom Airflow Docker image.
# Keep versions pinned to avoid breakage across rebuilds.
# ============================================================

# ── dbt ──────────────────────────────────────────────────────
dbt-bigquery==1.8.2
dbt-core==1.8.2

# ── Data Quality ─────────────────────────────────────────────
great-expectations==0.18.19

# ── GCP ──────────────────────────────────────────────────────
google-cloud-bigquery==3.25.0
google-cloud-bigquery-storage==2.25.0
google-cloud-storage==2.18.2
google-cloud-pubsub==2.23.0
pyarrow==16.1.0
db-dtypes==1.3.0

# ── Data manipulation ─────────────────────────────────────────
pandas==2.2.2

# ── Synthetic data (ingestion scripts) ───────────────────────
faker==28.4.1
requests==2.32.3

# ── Airflow providers ─────────────────────────────────────────
apache-airflow-providers-google==10.22.0
apache-airflow-providers-celery==3.7.0
